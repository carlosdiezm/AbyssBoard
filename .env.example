HF_TOKEN=hf_...
# Optional: override the default free instruct model used by the chat assistant.
# Default (microsoft/Phi-3-mini-4k-instruct) is ungated and has a live Inference
# Provider as of this writing. If it starts 502ing, check
# https://huggingface.co/api/models/{model}?expand[]=inferenceProviderMapping
# for a model with a "status":"live" entry and set HF_MODEL to that.
# HF_MODEL=microsoft/Phi-3-mini-4k-instruct
